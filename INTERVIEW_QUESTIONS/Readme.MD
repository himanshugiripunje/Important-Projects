# How to add a post build action in the pipeline in Jenkins
                    
                 -  post {
                    always {
                      echo ‘I will always execute this!’
                    }
                  }
